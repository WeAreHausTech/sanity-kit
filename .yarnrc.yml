nodeLinker: node-modules

npmRegistryServer: "https://registry.npmjs.org"

npmScopes:
  haus-tech:
    npmAuthToken: "${NODE_AUTH_TOKEN}"
    npmRegistryServer: "https://registry.npmjs.org"
  haus-storefront-react:
    npmAuthToken: "${NODE_AUTH_TOKEN}"
    npmRegistryServer: "https://registry.npmjs.org"

enableScripts: false

npmMinimalAgeGate: 10080 # 7 dagar

#npmPreapprovedPackages:
#  - "@types/*"
#  - "typescript"
